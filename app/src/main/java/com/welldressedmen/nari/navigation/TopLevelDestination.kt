package com.welldressedmen.nari.navigation

enum class TopLevelDestination {
    USER_DATA,
    MAIN_CONTENT,
}

enum class UserDataDestination {
    LOGIN,
    Survey,
}

enum class MainContentDestination {
    HOME,
    PROFILE,
    SETTINGS,
}